// create a variable called multiply set to an equation that will multiply variable num1 and num2 to equal 62
let num1 = 62
let num2 = 1

const multiply = num1 * num2;

// create a variable called random that will generate a random integer greater than 0
const random = Math.floor(Math.random() * 21)

//create a variable called mod set to an equation that will calculate the remainder of dividing variable num3 by num4; the remainder should be 4
let num3 = 12 
let num4 = 3

const mod = num3 / num4


//creater a variable called max that finds the highest number in a set; the value returned should be 20

const max = Math.max(1,2,3,4,20);